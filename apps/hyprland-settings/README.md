# hyprland-settings (vendored)

This is a **vendored copy** bundled into Sarwar's ArchDotfiles so the whole
setup installs from a single local `install.sh` with no external downloads.

It is installed automatically from this local copy — do **not** clone it.
See the repository root [README](../../README.md) for details.
